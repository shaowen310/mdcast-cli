# mdcast-cli

A Python CLI tool to convert Markdown to DOCX and vice versa.

## Status

Project scaffolding in progress. Implementation has not started yet.

## Roadmap

- `docx2md` converter (placeholder — existing project will be migrated in)
- Markdown -> DOCX conversion
- DOCX -> Markdown conversion
- Shared utilities and format-specific helpers
